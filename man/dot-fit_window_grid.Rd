% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/systemsim.R
\name{.fit_window_grid}
\alias{.fit_window_grid}
\title{Fit the refit specs on a deterministic window-end grid}
\usage{
.fit_window_grid(
  sys,
  specs,
  refit_idx,
  taus,
  window,
  width,
  nsim,
  globals = NULL
)
}
\arguments{
\item{sys}{A trimmed \code{endogenr_system_setup} (no simulation grid) carrying
the fit inputs (\code{train_data}, \code{full_data}, \code{fit_ctx}, \code{train_start}, ...).}

\item{specs}{The list of model specs.}

\item{refit_idx}{Integer positions of the refit specs within \code{specs}.}

\item{taus}{Integer window-end anchors from \code{.cf_anchor_grid()}.}

\item{window}{\code{"rolling"} or \code{"expanding"}.}

\item{width}{Integer rolling-window length.}

\item{nsim}{Integer number of draws.}

\item{globals}{Optional named list of user functions for parallel workers.}
}
\value{
A list aligned with \code{specs}: \code{NULL} at non-refit positions, and at
each refit position a list over windows, each a length-\code{nsim} list of
fitted models.
}
\description{
For the sliding-window mode of \code{\link[=fit_system]{fit_system()}}: refits each \code{linear}/\code{glm}/
\code{heterolm} spec on every window anchor in \code{taus}. Each window is fit \code{nsim}
times when the spec carries a \code{boot} resampler (distinct draws) and once
(shared across draws) otherwise. Parallelised over windows via the \code{future}
package.
}
\keyword{internal}
