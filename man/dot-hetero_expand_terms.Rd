% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{.hetero_expand_terms}
\alias{.hetero_expand_terms}
\title{Pre-expand interaction/factor terms for heterolm via model.matrix}
\usage{
.hetero_expand_terms(clean_labels, mat_data)
}
\arguments{
\item{clean_labels}{Character vector of cleaned term labels (from
\code{.clean_term_labels()}). May include interaction labels like \code{"a:b"}.}

\item{mat_data}{A data.table, modified in-place by reference.}
}
\value{
A list with:
\itemize{
\item \code{col_names}: flat cleaned column names (excluding intercept) to use in
the heterolm formula (e.g. \code{c("g_b", "x", "g_b_x")}).
\item \code{rhs_form}: the reformulated RHS formula (for re-use in predict).
}
}
\description{
\code{heterolm::hetero()} uses column-name lookups rather than R's formula
algebra, so interaction terms (\code{a:b}) and factor dummy codes must be
pre-computed as named columns. This helper runs \code{model.matrix} on the
cleaned-label formula (which uses the already-materialized column names),
adds any new columns to \code{mat_data} \strong{in-place} (data.table reference
semantics), and returns the flat column names for use in the heterolm
formula string.
}
\keyword{internal}
