% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{.model_frame_by_group}
\alias{.model_frame_by_group}
\title{Per-group model.frame with optional key-column injection}
\usage{
.model_frame_by_group(formula_eval, data, all_keys, needs_keys)
}
\arguments{
\item{formula_eval}{A prepared formula (positional lag injected, index
appended).}

\item{data}{A data.table.}

\item{all_keys}{Character vector of grouping columns (from \code{\link[=ctx_keys]{ctx_keys()}}).}

\item{needs_keys}{Logical. When \code{TRUE}, recycle key values into the
per-group frame.}
}
\value{
A data.table with \code{all_keys} + materialized columns.
}
\description{
Evaluates \code{formula_eval} per group via \code{model.frame()}. When
\code{needs_keys = TRUE}, the scalar group-key values are recycled into the
per-group data frame before evaluation so that formula terms that
reference a key column (e.g. \code{factor(unit)}) can be evaluated.
}
\keyword{internal}
