% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/systemsim.R
\name{.strip_fit_data}
\alias{.strip_fit_data}
\title{Drop the cached training frame and fitting-only payloads from a fitted model}
\usage{
.strip_fit_data(model)
}
\arguments{
\item{model}{A fitted endogenmodel.}
}
\value{
The model with fitting-only payloads removed.
}
\description{
Regression model objects (\code{linear}/\code{glm}/\code{heterolm}) cache the full
materialized training frame in \verb{$data} for fitting only; \verb{predict.*} never
reads it. Additionally strips large fitting-only fields from the inner
\verb{$fitted} object:
\itemize{
\item \code{lm}: removes \verb{$model}, \verb{$effects}, \verb{$fitted.values}.
\item \code{glm}: additionally removes \verb{$linear.predictors} and \verb{$y}.
\item \code{hetero_fit}: removes \verb{$frame$X}, \verb{$frame$Z}, \verb{$frame$y}; retains the
normalization vectors (\code{X_center}/\code{X_scale}/\code{Z_center}/\code{Z_scale}) and
\verb{$frame$parsed} needed by \code{predict.hetero_fit(newdata = ...)}.
}
}
\keyword{internal}
