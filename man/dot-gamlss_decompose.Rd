% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gamlssmodel.R
\name{.gamlss_decompose}
\alias{.gamlss_decompose}
\title{Decompose gamlss formulas into a smoother/grouping-free graph formula}
\usage{
.gamlss_decompose(
  formula,
  sigma.formula = NULL,
  nu.formula = NULL,
  tau.formula = NULL
)
}
\arguments{
\item{formula}{Main two-sided gamlss formula (mu; may contain pb/cs/lo,
random/ra/re).}

\item{sigma.formula}{Optional one-sided sigma formula.}

\item{nu.formula}{Optional one-sided nu formula.}

\item{tau.formula}{Optional one-sided tau formula.}
}
\value{
A list with:
\item{outcome}{Character: the LHS variable name.}
\item{graph_formula}{A formula suitable for dependency analysis.}
}
\description{
Extracts every predictor variable (for dependency-graph edges) from the four
parameter formulas (mu/sigma/nu/tau). Grouping factors from random()/ra()/re()
are treated as ordinary predictors: they are read at every forecast step, so
they must be produced by some model (e.g. an \code{exogen} carrying the grouping
column forward). Returns a graph_formula that \code{stats::terms()} and
\code{.edges_from_formula()} can consume safely (no bars, no random() calls).
}
\keyword{internal}
