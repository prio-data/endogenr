% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/longhorizon.R
\name{lead_horizon}
\alias{lead_horizon}
\title{Lead a vector by a forecast horizon (positional within-group shift)}
\usage{
lead_horizon(x, h = 1L)
}
\arguments{
\item{x}{A vector, assumed sorted in time order within a group.}

\item{h}{Integer. Number of steps to lead.}
}
\value{
A vector the same length and type as \code{x}, led by \code{h} with \code{NA} padding.
}
\description{
Returns the value \code{h} steps ahead within an ordered vector, padding the tail
with \code{NA}. This is the long-horizon analogue of the positional \code{lag()} used in
simulation formulas, and the marker recognised on the left-hand side of
long-horizon formulas (see \code{\link[=setup_long_horizon]{setup_long_horizon()}}).
}
\details{
Inside a long-horizon formula, write the outcome on the LHS wrapped in
\code{lead_horizon()}, e.g. \code{lead_horizon(gdppc_grwt) ~ gdppc_grwt + log(gdppc)}.
The horizon \code{h} is supplied internally per horizon by \code{\link[=setup_long_horizon]{setup_long_horizon()}};
you do not pass it in the formula. The right-hand side is evaluated at the
forecast origin (the last observed period), so write \code{lag()} only when you
deliberately want older history.
}
\examples{
lead_horizon(1:5, 2)
}
\seealso{
Other long_horizon:
\code{\link[=compare_approaches]{compare_approaches()}},
\code{\link[=create_lh_data]{create_lh_data()}},
\code{\link[=cv_long_horizon]{cv_long_horizon()}},
\code{\link[=fit_lh_model]{fit_lh_model()}},
\code{\link[=forecast_long_horizon]{forecast_long_horizon()}},
\code{\link[=get_lh_accuracy]{get_lh_accuracy()}},
\code{\link[=setup_long_horizon]{setup_long_horizon()}}
}
\concept{long_horizon}
