% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experiments.R
\name{window_config}
\alias{window_config}
\title{Build a window configuration for \code{run_experiments()}}
\usage{
window_config(
  window = c("random", "rolling", "expanding"),
  width = NULL,
  step = 1L,
  window_policy = c("latest", "equal", "decay"),
  decay = 0.5,
  weights = NULL
)
}
\arguments{
\item{window}{One of \code{"random"} (default, historical behaviour),
\code{"rolling"} (fixed-width sliding window), or \code{"expanding"} (expanding
window from \code{train_start}). Passed to \code{\link[=fit_system]{fit_system()}}.}

\item{width}{Integer or \code{NULL}. Rolling-window length; only used when
\code{window = "rolling"}. Defaults to \code{min_window} (or 10) inside
\code{\link[=fit_system]{fit_system()}}.}

\item{step}{Positive integer. Spacing between window-end anchors for sliding
modes. Default \code{1L}. Ignored for \code{"random"}.}

\item{window_policy}{One of \code{"latest"} (default), \code{"equal"}, or \code{"decay"}.
Controls which training window drives each forecast step in sliding modes;
ignored for \code{"random"}. Passed to \code{\link[=simulate_system]{simulate_system()}}.}

\item{decay}{Numeric in \verb{(0, 1]}. Per-step recency retention for
\code{window_policy = "decay"} at horizon 1. Default \code{0.5}.}

\item{weights}{Optional custom window weights overriding \code{window_policy}: a
\verb{function(h, age)} or a \verb{horizon x n_window} matrix. Ignored for
\code{"random"}.}
}
\value{
An \code{endogenr_window} object (a list with six fields).
}
\description{
Creates a lightweight configuration object bundling the fit-side window
approach (passed to \code{\link[=fit_system]{fit_system()}}) and the sim-side window policy (passed to
\code{\link[=simulate_system]{simulate_system()}}). Pass a single config to apply it uniformly across all
experiments, or a \strong{named list} of configs to \code{\link[=run_experiments]{run_experiments()}} to cross
window approach as a fourth grid axis alongside \code{models}, \code{train_start}, and
\code{test_start}.
}
\examples{
# Default: random windows, latest policy (historical behaviour)
window_config()

# Rolling window with decay policy
window_config("rolling", width = 15, step = 2,
              window_policy = "decay", decay = 0.7)

# Named list as a grid axis in run_experiments()
\dontrun{
run_experiments(
  data = df, models = my_system,
  train_start = 1970, test_start = 2010, horizon = 10,
  groupvar = "gwcode", timevar = "year",
  inner_sims = 10, nsim = 20, min_window = 20,
  windows = list(
    rand = window_config(),
    roll = window_config("rolling", width = 20, window_policy = "latest")
  )
)
}
}
\seealso{
\code{\link[=run_experiments]{run_experiments()}}, \code{\link[=fit_system]{fit_system()}}, \code{\link[=simulate_system]{simulate_system()}}

Other experiments:
\code{\link[=get_experiment_accuracy]{get_experiment_accuracy()}},
\code{\link[=run_experiments]{run_experiments()}},
\code{\link[=vary_model]{vary_model()}}
}
\concept{experiments}
