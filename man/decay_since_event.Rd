% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula_helpers.R
\name{decay_since_event}
\alias{decay_since_event}
\title{Exponential decay since event}
\usage{
decay_since_event(event, lambda = 0.5, max_years = 50)
}
\arguments{
\item{event}{A numeric vector. An event is considered to occur when
\code{event > 0}.}

\item{lambda}{Decay rate. Larger values mean faster decay.
Half-life in time units is \code{log(2) / lambda}.}

\item{max_years}{The assumed time since last event for units with no
observed event. Also used as the floor for the decay.}
}
\value{
A numeric vector the same length as \code{event}.
}
\description{
Computes an exponentially decaying signal from the most recent occurrence
of an event. Intended for use inside model formulas in endogenr.
}
\details{
At the time of the event, the value is 1. It then decays as
\code{exp(-lambda * time_since_event)}. Before any event has occurred,
the value equals \code{exp(-lambda * max_years)} (i.e., as if the last
event happened \code{max_years} ago). The output is floored at this value
so that it never drops below the left-censored default.
}
\examples{
# In a formula:
# build_model("linear",
#   formula = y ~ lag(decay_since_event(conflict, 0.3)) + lag(log(gdppc)),
#   boot = "resid")

x <- c(0, 0, 1, 0, 0, 0, 1, 0, 0)
decay_since_event(x, lambda = 0.5)
}
\seealso{
Other formula_helpers:
\code{\link[=derive_naive_formula]{derive_naive_formula()}},
\code{\link[=inject_positional_lag]{inject_positional_lag()}},
\code{\link[=intensity_decay]{intensity_decay()}},
\code{\link[=materialize_formula]{materialize_formula()}},
\code{\link[=time_since_event]{time_since_event()}}
}
\concept{formula_helpers}
