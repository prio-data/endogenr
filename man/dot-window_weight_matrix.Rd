% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/systemsim.R
\name{.window_weight_matrix}
\alias{.window_weight_matrix}
\title{Build the per-step window-selection weight matrix}
\usage{
.window_weight_matrix(policy, horizon, taus, decay = 0.5, weights = NULL)
}
\arguments{
\item{policy}{One of \code{"latest"}, \code{"equal"}, \code{"decay"}.}

\item{horizon}{Integer number of forecast steps.}

\item{taus}{Integer window-end anchors (ascending).}

\item{decay}{Numeric in \verb{(0, 1]}. Per-step retention at horizon 1 for the
\code{"decay"} policy.}

\item{weights}{Optional override: a \verb{function(h, age)} returning unnormalised
weights, or a \verb{horizon x n_window} numeric matrix.}
}
\value{
A row-normalised \verb{horizon x n_window} numeric matrix.
}
\description{
Produces a \verb{horizon x n_window} matrix of selection probabilities used by the
sliding-window \code{\link[=simulate_system]{simulate_system()}}: row \code{h} gives the probability of choosing
each window at forecast step \code{h}. Windows are ordered as in \code{taus}
(ascending; the last is the most recent). \code{age} is \code{0} for the most recent
window and increases into the past.
}
\keyword{internal}
