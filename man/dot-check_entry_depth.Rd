% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/systemsim.R
\name{.check_entry_depth}
\alias{.check_entry_depth}
\title{Check that origin units can supply the models' history depth}
\usage{
.check_entry_depth(train, ctx, specs, test_start)
}
\arguments{
\item{train}{A data.table with the training rows (\code{time < test_start}).}

\item{ctx}{A panel_context object (unit/time, no sim).}

\item{specs}{A list of model specs from \code{\link[=build_model]{build_model()}}.}

\item{test_start}{Integer. First forecast time step.}
}
\value{
Invisible NULL; throws on violation.
}
\description{
Computes the maximum finite history depth required by any spec formula (and
heterolm variance formula) via \code{\link[=.required_history]{.required_history()}}, and errors when a unit
present at the forecast origin enters the panel too late to supply rows
\verb{[test_start - max_need, test_start - 1]} (contiguity is already validated).
Infinite needs (cumulative ts functions) are ignored: they consume whatever
history exists, by design (see \code{.history_subset()}).
}
\keyword{internal}
