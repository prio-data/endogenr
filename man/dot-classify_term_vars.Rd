% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/systemgraph.R
\name{.classify_term_vars}
\alias{.classify_term_vars}
\title{Classify the variables in a single formula term by lag context}
\usage{
.classify_term_vars(expr)
}
\arguments{
\item{expr}{A language object (one parsed formula term).}
}
\value{
A list with \code{lagged} and \code{plain} character vectors (each unique).
}
\description{
Walks the term's AST and records each variable as lagged (it appears inside
a \code{lag()} call) or same-period (it does not). The classification is
per-variable, not per-term: a same-period variable sharing a term with a
lagged one (e.g. \code{I(lag(y) * x)}) is still same-period. A variable that is
both (e.g. \code{I(x - lag(x))}) is reported in both buckets.
}
\keyword{internal}
