% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{.clean_term_labels}
\alias{.clean_term_labels}
\title{Rewrite formula term labels using a clean column-name mapping}
\usage{
.clean_term_labels(formula, col_mapping)
}
\arguments{
\item{formula}{An R formula.}

\item{col_mapping}{Named character vector mapping raw model.frame column
names to cleaned names (as returned by \code{\link[=.build_mat_cache]{.build_mat_cache()}}).}
}
\value{
Character vector of cleaned term labels (zero-length for
intercept-only or empty formulas).
}
\description{
Walks the \code{factors} matrix of \code{terms(formula)} and replaces each raw
variable token (e.g. \code{"lag(log(gdppc))"}, \code{"factor(region)"}) with its
cleaned column name from \code{col_mapping}. Interaction structure (\code{:}) is
preserved: a term that was \code{g:x} in the raw formula becomes
\code{clean(g):clean(x)} in the output. Variables absent from \code{col_mapping}
pass through unchanged.
}
\keyword{internal}
