% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{derive_naive_formula}
\alias{derive_naive_formula}
\title{Derive a naive formula preserving term structure}
\usage{
derive_naive_formula(formula, col_mapping, outcome = NULL)
}
\arguments{
\item{formula}{The original R formula (before materialization).}

\item{col_mapping}{Named character vector. Maps raw \code{model.frame()} column
names to cleaned names (as returned by \code{\link[=.build_mat_cache]{.build_mat_cache()}}).}

\item{outcome}{Character. Cleaned outcome column name. When \code{NULL}
(default), the LHS of \code{formula} is mapped through \code{col_mapping}.}
}
\value{
An R formula with cleaned variable names and full term structure.
}
\description{
Reconstructs a formula suitable for \code{lm()} / \code{glm()} by mapping each raw
variable token in the original formula to its cleaned materialized column
name (via \code{col_mapping}), while preserving all term structure: interactions
(\code{:}), crossing (\code{*}), intercept suppression (\code{-1} / \code{0}), and higher-order
terms. This is the only correct way to derive the naive formula because
additive column-name reconstruction (the old approach) silently drops
interaction terms.
}
\seealso{
Other formula_helpers:
\code{\link[=decay_since_event]{decay_since_event()}},
\code{\link[=inject_positional_lag]{inject_positional_lag()}},
\code{\link[=intensity_decay]{intensity_decay()}},
\code{\link[=materialize_formula]{materialize_formula()}},
\code{\link[=time_since_event]{time_since_event()}}
}
\concept{formula_helpers}
