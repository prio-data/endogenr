% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glmmTMBmodel.R
\name{.glmmTMB_decompose}
\alias{.glmmTMB_decompose}
\title{Decompose a glmmTMB formula into a bar-free graph formula}
\usage{
.glmmTMB_decompose(formula, dispformula = NULL, ziformula = NULL)
}
\arguments{
\item{formula}{Main two-sided glmmTMB formula (may contain RE bars,
cov-struct wrappers).}

\item{dispformula}{Optional one-sided dispersion formula.}

\item{ziformula}{Optional one-sided zero-inflation formula.}
}
\value{
A list with:
\item{outcome}{Character: the LHS variable name.}
\item{graph_formula}{A bar-free formula for dependency analysis.}
}
\description{
Extracts every predictor variable (for dependency-graph edges) from the main
formula plus dispformula/ziformula. Random-effects grouping factors and
covariance-structure coordinates are treated as ordinary predictors: they are
read at every forecast step, so — like any predictor — they must be produced
by some model (e.g. an \code{exogen} that carries the grouping column forward).
Returns a bar-free graph_formula that \code{stats::terms()} and
\code{.edges_from_formula()} can consume safely.
}
\keyword{internal}
