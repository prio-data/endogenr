% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{materialize_formula}
\alias{materialize_formula}
\title{Materialize formula terms into a data.table}
\usage{
materialize_formula(
  formula,
  data,
  ctx,
  .mat_formula = NULL,
  .col_mapping = NULL
)
}
\arguments{
\item{formula}{An R formula.}

\item{data}{A data.table (or data.frame/tsibble — will be coerced).}

\item{ctx}{A \code{panel_context} object.}

\item{.mat_formula}{Optional. A pre-prepared formula (already has index var
appended and positional lag injected). When provided, \code{formula} is ignored
for evaluation and used only as documentation.}

\item{.col_mapping}{Optional named character vector. Maps raw \code{model.frame()}
column names to clean names. When provided, \code{janitor::clean_names()} is
skipped and \code{setnames()} is used instead.}
}
\value{
A data.table with cleaned column names.
}
\description{
Evaluates formula terms per group using \code{model.frame()} with positional lag
injection. Returns the materialized data.table with cleaned column names.
}
\details{
For repeated calls with the same formula (e.g. inside a simulation loop),
pass \code{.mat_formula} and \code{.col_mapping} (cached from a prior call) to skip
the \code{update()} / \code{inject_positional_lag()} / \code{clean_names()} overhead.
}
\seealso{
Other formula_helpers:
\code{\link[=decay_since_event]{decay_since_event()}},
\code{\link[=derive_naive_formula]{derive_naive_formula()}},
\code{\link[=inject_positional_lag]{inject_positional_lag()}},
\code{\link[=intensity_decay]{intensity_decay()}},
\code{\link[=time_since_event]{time_since_event()}}
}
\concept{formula_helpers}
