% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/systemsim.R
\name{.fit_spec}
\alias{.fit_spec}
\title{Fit a single spec for the system}
\usage{
.fit_spec(spec, sys, subset = NULL)
}
\arguments{
\item{spec}{An \code{endogenr_spec} from \code{\link[=build_model]{build_model()}}.}

\item{sys}{An \code{endogenr_system_setup} (or trimmed copy) carrying
\code{fit_ctx}, \code{train_data}, \code{full_data}, \code{test_start}, and \code{inner_sims}.}

\item{subset}{Optional list with \code{start}/\code{end} for a training window,
applied to the \code{linear}/\code{glm}/\code{heterolm}/\code{glmmTMB}/\code{gamlss} families.}
}
\value{
A fitted endogenmodel object.
}
\description{
Dispatches a single model spec to \code{\link[=fit_model]{fit_model()}} with the arguments each
type expects, reading the shared training/forecast data and contexts from
the setup object. Extracted so it can be called once for the shared
baseline and once per refit draw.
}
\keyword{internal}
