% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/systemgraph.R
\name{.required_history}
\alias{.required_history}
\title{Compute the history depth a formula's RHS needs before time \code{t}}
\usage{
.required_history(formula)
}
\arguments{
\item{formula}{An R formula (its RHS is walked) or a language object.}
}
\value{
A non-negative numeric scalar, possibly \code{Inf}.
}
\description{
Walks the right-hand-side AST and composes nested time-series depths so that
materialising the RHS over the \code{need} rows ending at \code{t} reproduces the value
it would take over the full per-unit series. Unlike \code{\link[=.max_lag_depth]{.max_lag_depth()}} (which
takes the maximum of individual depths), nested transforms are summed, so
\code{lag(lag(x))} needs 2 and \code{lag(rollmeanr(x, 5), 2)} needs 6.
}
\details{
Composition rules:
\itemize{
\item \code{lag(expr, n)} / \code{shift(expr, n)} -> \code{n + required(expr)} (default
\code{n = 1}; \code{shift(type = "lead")} is forward, contributing only
\code{required(expr)}).
\item \code{lead(expr, n)} / \code{lead_horizon(expr, h)} -> \code{required(expr)}.
\item \code{diff(expr, lag, differences)} -> \code{lag * differences + required(expr)}
(defaults \verb{1, 1}).
\item rolling (\verb{roll*}, \verb{froll*}) -> \code{required(expr)} plus the window's
backward reach (right: \code{k - 1}, center: \code{floor(k / 2)}, left: \code{0}).
Alignment defaults follow the function name and an explicit \code{align}
argument overrides it.
\item \code{cumsum}/\code{cumprod}/\code{cummax}/\code{cummin} and the \verb{*_since_event} helpers
depend on the entire prior series -> \code{Inf}.
\item any other call -> max over its arguments; symbols/literals -> \code{0}.
}

An undeterminable lag count or rolling window yields \code{Inf} (use the full
series), which never under-reads.
}
\keyword{internal}
