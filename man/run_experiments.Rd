% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experiments.R
\name{run_experiments}
\alias{run_experiments}
\title{Run a grid of system-simulation experiments}
\usage{
run_experiments(
  data,
  models,
  train_start,
  test_start,
  horizon,
  groupvar,
  timevar,
  inner_sims,
  nsim = 1L,
  min_window = NULL,
  windows = NULL,
  globals = NULL,
  parallel = c("experiments", "draws"),
  keep_fits = FALSE
)
}
\arguments{
\item{data}{A data.frame, data.table, or tsibble; see \code{\link[=setup_system]{setup_system()}}.}

\item{models}{Either a single model system (a list of \code{\link[=build_model]{build_model()}} specs)
or a named list of such systems. \code{\link[=vary_model]{vary_model()}} is the ergonomic way to
build the named list by swapping individual specs.}

\item{train_start}{Integer scalar or vector of training starts to cross.}

\item{test_start}{Integer scalar or vector of forecast starts to cross.}

\item{horizon}{Integer. Forecast horizon, shared across experiments.}

\item{groupvar, timevar}{Character. Panel unit and time column names.}

\item{inner_sims}{Integer. Inner simulations per draw; see \code{\link[=setup_system]{setup_system()}}.}

\item{nsim}{Integer. Number of coefficient draws per experiment; see
\code{\link[=fit_system]{fit_system()}}.}

\item{min_window}{Integer or \code{NULL}. Random-window refitting; see
\code{\link[=setup_system]{setup_system()}}.}

\item{windows}{Either \code{NULL} (default; random window, latest policy),
a single \code{\link[=window_config]{window_config()}} applied to every experiment, or a named list
of \code{\link[=window_config]{window_config()}} objects that is crossed as a fourth grid axis. The
config name appears as the \code{window_cfg} column.}

\item{globals}{Named list of user functions referenced by formulas, exported
to parallel workers; see \code{\link[=setup_system]{setup_system()}}.}

\item{parallel}{One of \code{"experiments"} (default) or \code{"draws"}; see the
Parallelism section.}

\item{keep_fits}{Logical. When \code{TRUE}, the per-experiment
\code{endogenr_fitted_system} objects are attached to the result as a \code{fits}
attribute (named by \code{.experiment}), so \code{\link[=get_coefficients]{get_coefficients()}} /
\code{\link[=plot_coefficients]{plot_coefficients()}} can be run per experiment. Default \code{FALSE} keeps the
result lean.}
}
\value{
A \code{data.table} stacking every experiment's \code{\link[=simulate_system]{simulate_system()}}
output, with leading columns \code{.experiment}, \code{model}, \code{train_start},
\code{test_start}, \code{window_cfg} and the usual simulation columns plus \code{.sim}.
Carries \code{panel_unit}, \code{panel_time}, and \code{experiments} attributes (and
\code{fits} when \code{keep_fits = TRUE}).
}
\description{
Runs the \code{\link[=setup_system]{setup_system()}} -> \code{\link[=fit_system]{fit_system()}} -> \code{\link[=simulate_system]{simulate_system()}} pipeline
once per experiment across a grid of variations and stacks the results into a
single long \code{data.table}. Vary the model system, the training start, and/or
the forecast start; every combination is a separate experiment with a
readable label. The output carries the same \code{panel_unit} / \code{panel_time}
attributes as \code{\link[=simulate_system]{simulate_system()}}, so it plugs directly into
\code{\link[=get_experiment_accuracy]{get_experiment_accuracy()}} and \code{\link[=plotsim]{plotsim()}}.
}
\details{
The experiment grid is the Cartesian product of up to four axes: the model
systems (\code{models}), \code{train_start}, \code{test_start}, and optionally \code{windows}
(see \code{\link[=window_config]{window_config()}}). An axis with a single value does not expand.
\code{horizon}, \code{inner_sims}, \code{min_window}, and \code{nsim} are shared across every
experiment.

Each experiment is stamped with five leading columns: \code{.experiment} (a unique
label built only from the axes that vary), \code{model}, \code{train_start},
\code{test_start}, and \code{window_cfg} (the name of the window configuration used).
The result also carries an \code{experiments} attribute (a metadata \code{data.table})
recording the configuration of every experiment, which
\code{\link[=get_experiment_accuracy]{get_experiment_accuracy()}} uses to recover per-experiment metadata.
}
\section{Parallelism}{

Because \code{future} evaluates \emph{nested} futures sequentially unless a multi-level
plan is set, you set \strong{one} \code{\link[future:plan]{future::plan()}} and choose where it bites with
\code{parallel}:
\itemize{
\item \code{"experiments"} (default): the experiment grid is mapped in parallel
and each experiment's internal \code{fit}/\code{simulate} runs sequentially on its
worker. Best when there are many experiments. Each worker builds its own
simulation grid, so memory scales with the number of workers — keep that
in mind for large panels.
\item \code{"draws"}: experiments run sequentially and each \code{\link[=fit_system]{fit_system()}} /
\code{\link[=simulate_system]{simulate_system()}} parallelises over its \code{nsim} draws (the single-
experiment behaviour). Best when there are few experiments but a large
\code{nsim}.
}
Set the plan yourself, e.g. \code{future::plan(future::multisession, workers = 8)}.
Reproducibility uses \code{future.seed = TRUE}; seed once with \code{\link[=set.seed]{set.seed()}} before
calling. The two schemes lay out their random streams differently, so they
are reproducible \emph{within} a scheme but not bit-identical \emph{across} schemes (the
same caveat as \code{\link[=fit_system]{fit_system()}} versus the legacy single-call simulator).
}

\examples{
\dontrun{
df <- endogenr::example_data
base <- list(
  build_model("deterministic",
              formula = gdppc ~ I(abs(lag(gdppc) * (1 + gdppc_grwt)))),
  build_model("linear", formula = gdppc_grwt ~ lag(log(gdppc)), boot = "resid"),
  build_model("exogen", formula = ~population)
)
systems <- vary_model(base, list(
  e1 = build_model("linear", formula = gdppc_grwt ~ lag(log(gdppc)), boot = "resid"),
  e2 = build_model("linear", formula = gdppc_grwt ~ lag(log(gdppc)) + lag(population),
                   boot = "resid")
))

future::plan(future::multisession, workers = 8)
set.seed(42)
res <- run_experiments(
  data = df, models = systems,
  train_start = 1970, test_start = c(2005, 2010), horizon = 12,
  groupvar = "gwcode", timevar = "year",
  inner_sims = 30, nsim = 64, min_window = 20
)
future::plan(future::sequential)

acc <- get_experiment_accuracy(res, "gdppc", df)
}
}
\seealso{
\code{\link[=vary_model]{vary_model()}}, \code{\link[=window_config]{window_config()}}, \code{\link[=setup_system]{setup_system()}}, \code{\link[=fit_system]{fit_system()}},
\code{\link[=simulate_system]{simulate_system()}}, \code{\link[=get_experiment_accuracy]{get_experiment_accuracy()}}

Other experiments:
\code{\link[=get_experiment_accuracy]{get_experiment_accuracy()}},
\code{\link[=vary_model]{vary_model()}},
\code{\link[=window_config]{window_config()}}
}
\concept{experiments}
