% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula_helpers.R
\name{intensity_decay}
\alias{intensity_decay}
\title{Intensity-weighted exponential decay since event}
\usage{
intensity_decay(event, intensity, lambda = 0.5, max_years = 50)
}
\arguments{
\item{event}{A numeric vector. An event occurs when \code{event > 0}.}

\item{intensity}{A numeric vector of event intensities (e.g., fatalities).
Only used when \code{event > 0}.}

\item{lambda}{Decay rate.}

\item{max_years}{Used to set the floor per-event contribution.}
}
\value{
A numeric vector the same length as \code{event}.
}
\description{
Like \code{\link{decay_since_event}}, but the peak value equals the
event intensity (e.g., fatalities) rather than 1. Multiple events
accumulate: each past event contributes its own decaying intensity.
}
\examples{
evt <- c(0, 1, 0, 0, 1, 0)
fat <- c(0, 100, 0, 0, 500, 0)
intensity_decay(evt, fat, lambda = 0.3)
}
