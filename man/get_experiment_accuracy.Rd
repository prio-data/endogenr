% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experiments.R
\name{get_experiment_accuracy}
\alias{get_experiment_accuracy}
\title{Score a grid of system-simulation experiments}
\usage{
get_experiment_accuracy(
  experiment_results,
  outcome,
  truth,
  by = NULL,
  level = 50,
  transform = NULL
)
}
\arguments{
\item{experiment_results}{A \code{data.table} from \code{\link[=run_experiments]{run_experiments()}}.}

\item{outcome}{Character. Name of the outcome variable to score.}

\item{truth}{A data.frame/data.table of observed values; must contain the
group, time, and outcome columns.}

\item{by}{Character vector of grouping columns. Defaults to
\verb{c(<unit>, "horizon")}, matching \code{\link[=get_lh_accuracy]{get_lh_accuracy()}} / \code{\link[=compare_approaches]{compare_approaches()}}.}

\item{level}{Numeric. Coverage level for the Winkler score (default 50).}

\item{transform}{Function or \code{NULL}. Applied to both draws and truth before
scoring; see \code{\link[=get_accuracy]{get_accuracy()}}.}
}
\value{
A \code{data.table} with leading columns \code{.experiment} and any present
axis columns (\code{model}, \code{train_start}, \code{test_start}), the \code{by} columns, and
\code{crps}, \code{mae}, \code{winkler} — one block of rows per experiment.
}
\description{
Computes CRPS, MAE, and Winkler scores for every experiment in a
\code{\link[=run_experiments]{run_experiments()}} result, returning one long \code{data.table} keyed by
\code{.experiment}. Each experiment is scored against \code{truth} with its \strong{own}
\code{test_start}, so a grid that varies \code{test_start} is handled correctly (the
\code{horizon} column is \code{time - test_start + 1} per experiment). This is the
experiment-aware analogue of \code{\link[=get_accuracy]{get_accuracy()}}, which it calls under the hood.
}
\details{
Rows before each experiment's \code{test_start} (the training period) are dropped
before scoring, so the reported horizons start at 1.
}
\seealso{
\code{\link[=run_experiments]{run_experiments()}}, \code{\link[=get_accuracy]{get_accuracy()}}, \code{\link[=compare_approaches]{compare_approaches()}}

Other experiments:
\code{\link[=run_experiments]{run_experiments()}},
\code{\link[=vary_model]{vary_model()}}
}
\concept{experiments}
